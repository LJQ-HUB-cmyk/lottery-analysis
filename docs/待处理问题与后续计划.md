# 待处理问题 & 后续计划 & 决策记录

> 集中管理所有已知问题、计划功能、暂不启用模块和已否决方案。避免信息散落在多个文件中。
> 最后更新：2026-05-22

---

## 一、待处理问题（已知缺陷，暂不致命）

> 这些是真实存在的问题，但目前影响可控。解决后打 ✅。

### 数据源

- [ ] **js-lottery 排列三主源持续 fallback** — fallback 路径仅解析 10 条，影响历史数据完整性。建议采集失败响应样本到 `logs/source_samples/`
- [ ] **21:35/22:05 推送记录缺失无法回溯** — `send_log.jsonl` 不会回溯写入历史记录，仅从 2026-05-20 23:31 修复后开始记录 `len+preview`
- [ ] **push_state.json 不存在** — `--stdout + deliver=origin` 模式不创建 push_state.json（预期行为），该模式去重靠 send_log.jsonl + 文件锁

### 工程

- [ ] **服务器端实跑验收** — 预测/复盘 normal×2/复盘 final×2/KL8 预测/KL8 复盘 全部在 Linux 上跑通。Windows 本地已验证通过
- [ ] **`~/.hermes/scripts/` 同步** — `git pull` 后需 `cp scripts/push/*.sh ~/.hermes/scripts/`

### 部署

- [ ] **`~/.hermes/scripts/` 需手动同步** — 项目脚本改完后必须手动 `cp scripts/push/*.sh ~/.hermes/scripts/`（软链接被 Hermes 安全策略拦截）

---

## 二、后续需要处理（P1 优先级，近期可做）

> 这些是已经过讨论、确认有价值、等待条件成熟即可执行的项目。

### KL8 模块

- [ ] **`scripts/kl8/backtest.py` 回测脚本** — 等 `review_history >= 30` 条后建，对比 v0随机/v1热冷/v2分区/v3遗漏 4 策略表现
- [ ] **`scripts/kl8/rules.py` 统一奖级表** — 选一~选十标准奖金，替代 reviewer 硬编码
- [ ] **predictor 分区均衡约束** — `--zone-balance` 参数，避免候选池过度集中
- [ ] **metrics 命中率加权** — 中四 > 中三 > 中二，分值不同

### 评分引擎

- [ ] **评分权重系统调优** — `tune_weights.py` 已就绪（随机搜索 + Optuna 贝叶斯优化），等待 `review_history.csv` 积累 30 期数据后运行

### 推送 & 监控

- [ ] **KL8 check.py 异常主动推送** — 当前 `deliver=local`，异常时不会通知。可改为 `deliver=origin` 或包装成 `kl8_check_push.sh`

---

## 三、后续可以考虑新增（P2 优先级，数据积累后做）

> 功能方向已确认，但需要更多数据或当前不急。

### KL8 模块

- [ ] **多策略对比报告** — v0随机/v1热冷/v2分区/v3遗漏，30 天后跑一次完整对比
- [ ] **stats.py 全量遗漏表** — 当前只有 top10，扩展为全量 80 个号码
- [ ] **check.py `--stage` 参数** — 区分 predict/review/full 检查场景
- [ ] **推送模板加奇偶/大小/连号展示** — stats 已有数据，模板可扩展

### 工程化

- [ ] **遗漏计算真正向量化** — 去除 Python `for` 循环（当前 7600 行性能足够）
- [ ] **GitHub Actions 每日自动运行**（可选，目前 Hermes cron 已覆盖）
- [ ] **五码/六码组选扩展方案** — 从 Top30 提取高频数字池，展开组六/组三组合作为单注推荐的互补维度（方案已有，~30 行代码，详见 memory）

### 数据源

- [ ] **Scrapling 爬虫兜底源** — 方案已设计（见 [2026-05-22 改造记录](2026-05-22-项目稳定化改造记录.md)），等现有数据源连续故障时再接入。只做 KL8 第三数据源兜底，环境变量控制，默认关闭

---

## 四、暂不启用（已实现但不活跃）

> 这些代码已存在，但因为和主链路重叠、条件不成熟、或被降级而不在活跃使用中。

| 文件 | 原因 | 后续 |
|------|------|------|
| `scripts/kl8/strategy.py` | 4 策略统一接口（v0/v1/v2/v3），待回测评估后启用 | 等 backtest.py 建好后评估 |
| `scripts/filter_engine.py` | 轻量预过滤已降级，主要逻辑移至 scoring_engine.py 评分体系 | 保留，必要时恢复 |
| `scripts/push_review_event_if_needed.py` | 与主复盘链路功能重叠，且存在扫描全历史导致旧事件重复触发的风险 | 需重写后再启用 |
| `scripts/kl8/` 独立模块 | 不接入 run_daily.py，保持与 PLS/D3 隔离 | 设计决策，不改 |

---

## 五、不考虑做（明确拒绝，避免反复讨论）

> 以下方案已经过讨论并明确否决，保留原因记录以免未来重复评估。

| 方案 | 否决原因 | 来源 |
|------|---------|------|
| **LSTM/ML/深度学习预测号码** | 彩票无时间依赖，ML 不优于统计方法；服务器无 GPU（2核/3.5GB）；ROI 趋近随机策略 | CLAUDE.md / README.md / PROJECT_REVIEW.md |
| **KL8 接入 run_daily.py** | 独立模块设计不改，避免和 PLS/D3 耦合 | CLAUDE.md |
| **Streamlit 网页面板** | 飞书推送已够用，增加维护成本 | CLAUDE.md |
| **config/kl8.yaml 配置文件** | 常量少，硬编码更清晰 | CLAUDE.md |
| **Scrapling 爬虫（当前接入）** | 现有数据源已稳定，多一个依赖多一个故障点。方案已记录，故障时再接入 | 2026-05-22 讨论 |
| **统一改用 cwl.gov.cn API** | 服务器上返回 403（WAF 防护），不可用 | PROJECT_REVIEW.md |
| **requirements.txt 版本锁定** | 项目运行在 Hermes venv 上，保持版本开放可安装最新兼容版本 | PROJECT_REVIEW.md |
| **添加 plotly/seaborn/tqdm 依赖** | 当前代码未使用，matplotlib 已满足基础需求 | PROJECT_REVIEW.md |
| **软链接部署 Hermes 脚本** | Hermes 安全策略拦截指向 `~/.hermes/scripts/` 外部的软链接，改用实体文件复制 | 2026-05-21 实践结论 |
| **用 Grok 简化版替换 feature_engine/scoring_engine** | 删除了核心功能（数据检查、多样性惩罚、冷号补偿等）且引入了错误 | PROJECT_REVIEW.md |

---

## 六、已完成（从旧清单中勾掉）

> 已解决的历史项目，保留在此以便追溯。

### 2026-05-22（v2.15.0 第三轮：Windows 兼容 + 本地验证）

- [x] **Windows GBK emoji 崩溃** — 6 个文件代码日志 emoji 改 `[OK]`/`[WARN]`/`[ERR]`
- [x] **subprocess 缺 UTF-8 环境变量** — 6 个文件 `subprocess.run` 统一加 `PYTHONUTF8=1`/`PYTHONIOENCODING=utf-8`
- [x] **Windows 本地验证通过** — data_fetcher/run_daily/daily_review/lottery_review_job 全部 exit 0

### 2026-05-22（v2.15.0 第二轮：GPT 审查修复）

- [x] **shell set -e 异常时日志不完整** — 4 个 push shell 用 `set +e` 包裹 Python 调用
- [x] **lottery_review_job.py final 兜底 --force 绕过去重** — 改为 `--final-check`
- [x] **Job 过程日志混入 stdout** — 4 个 job 全部过程日志改 `print(..., file=sys.stderr)`
- [x] **文件换行格式风险** — 新建 `.gitattributes` 强制 LF 换行
- [x] **README 缺正式入口说明** — 新增"正式运行入口"章节

### 2026-05-22（v2.15.0 第一轮：Job 架构改造）

- [x] **预测链路 cron 统一** — 删除 14:30/14:35 旧任务，14:40 为单一 Job 入口（lottery_predict_job.py 自闭环：run_daily + source_health + hermes_push）
- [x] **KL8 复盘旧文件误推** — `kl8_review_job.py` 删旧文件+时间戳+期号三重校验
- [x] **PLS/D3 复盘失败被吞掉** — `daily_review.py` 返回值聚合，异常 exit 2 阻断推送
- [x] **去重从文本 hash 升级为业务键** — `hermes_push.py` 新增 `already_sent_by_key` + `--dedup-key`
- [x] **晚间三波任务重叠执行** — shell 顶部 `flock -n` 全流程锁
- [x] **cron 推送 --force 绕过去重** — 4 个 push shell 全部去掉 `--force`
- [x] **Shell 脚本臃肿** — 4 个 shell 瘦身为薄入口，业务逻辑迁移到 4 个 Python job
- [x] **状态不可追踪** — 新增 `output/status/*.json` + `job_status.py`
- [x] **HERMES_CONFIG.md 路径不一致** — `/path/to` 全部替换为真实路径
- [x] **文档过时** — CHANGELOG / CLAUDE / README / Makefile 全部同步到 v2.15.0

### 2026-05-21（v2.14.0）

- [x] **KL8 推送脚本文件缺失** — `~/.hermes/scripts/` 新建实体文件
- [x] **lottery_review_push.sh 旧版未同步** — 同步新版（日志/--complete-only/--final-check）
- [x] **软链接被 Hermes 拦截** — 改为实体文件复制，确立维护方式

### 2026-05-21（v2.13.0）

- [x] **晚间复盘完整性闸门** — 双彩种齐全才推送，--complete-only/--final-check 区分波浪
- [x] **文件锁 stale_after 优化** — 拆分为 5s 等待 / 600s 过期，防止并发重复推送
- [x] **review_push.sh stderr 丢弃** — 重写为追加入日志文件

---

> **维护说明：** 此文件应随项目演进持续更新。新增想法时先在对应章节加 `- [ ]`，完成时打 `- [x]` 并移至"已完成"章节。否决方案时加入"不考虑做"章节并注明原因。
