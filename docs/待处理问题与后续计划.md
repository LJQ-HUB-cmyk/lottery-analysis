# 待处理问题 & 后续计划 & 决策记录

> 集中管理所有已知问题、计划功能、暂不启用模块和已否决方案。避免信息散落在多个文件中。
> 最后更新：2026-05-23（v2.15.2：P1 评分修复 + 止损护栏 + 健康检查优化）

---

## 一、待处理问题（已知缺陷，暂不致命）

> 这些是真实存在的问题，但目前影响可控。解决后打 ✅。

### 数据源

- [x] **js-lottery 排列三主源持续 fallback** — 已确认 fallback 增量追加机制正常（7607条数据正确）。健康检查已优化：主源异常但数据质量正常时标注"fallback生效"而非报警。未来恶化时再采失败样本到 `logs/source_samples/`
- [x] **PLS 7591行无日期列** — 已降级：`feature_engine.py` 中从 ❌异常 改为 ⚠️警告，文案注明"历史种子数据格式问题，已按期号排序继续处理，不影响评分"
- [x] **PyPI 官方源不可用** — 已规避：Hermes 统一走 `.venv/bin/python`，不碰系统 Python；新增依赖用 `uv + 阿里云镜像`。详见 `docs/HERMES_CONFIG.md` → "Python 环境约定"
- [ ] **21:35/22:05 推送记录缺失无法回溯** — `send_log.jsonl` 不会回溯写入历史记录，仅从 2026-05-20 23:31 修复后开始记录 `len+preview`
- [ ] **备份源（sporttery/zhcw）未实战验证** — 主源稳定，从未触发 fallback。等主源故障时自然验证即可

### 工程

- [x] **服务器端实跑验收** — 2026-05-22 已跑通，详见六、已完成章节。完整测试记录见 `output/tests/runtime_test_record.md`
- [x] **`~/.hermes/scripts/` 同步** — `git pull` 后已执行 `cp scripts/push/*.sh ~/.hermes/scripts/`，5个脚本 md5 完全一致

### 部署

- [x] **~/.hermes/scripts/ 需手动同步** — 已完成同步

### 测试记录

> 完整运行测试记录已归档至 `output/tests/runtime_test_record.md`，包含 21 项测试结果和 7 个问题记录。

## 二、后续需要处理（P1 优先级，近期可做）

> 这些是已经过讨论、确认有价值、等待条件成熟即可执行的项目。

### KL8 模块

- [ ] **`scripts/kl8/backtest.py` 回测脚本** — 等 `review_history >= 30` 条后建，对比 v0随机/v1热冷/v2分区/v3遗漏 4 策略表现
- [ ] **`scripts/kl8/rules.py` 统一奖级表** — 选一~选十标准奖金，替代 reviewer 硬编码
- [ ] **predictor 分区均衡约束** — `--zone-balance` 参数，避免候选池过度集中
- [ ] **metrics 命中率加权** — 中四 > 中三 > 中二，分值不同

### 评分引擎

- [ ] **PLS 权重调优** — `tune_weights.py` 已加止损护栏（ROI<随机中位数 -5、ROI<-10% -10、连未>20 -3）+ 随机多seed基线 + 候选文件输出（不覆盖正式权重）。复盘数据已达 42 期，可运行：
  ```bash
  .venv/bin/python scripts/tune_weights.py --lottery pls --method optuna --trials 80 --periods 80 --top-k 30
  ```
  上线前提：候选权重必须通过 ROI≥随机中位数 + 跨窗口稳定性检查。通过后人工 `cp` 到 `rules/` 目录。PLS 回测动态评分 ROI -42.3% vs 随机 +6.8%，调优后需对比确认改善再上线
- [x] **PLS 评分展示错位** — `376(367)` → `直选376｜组选367`，已修（`f7c2961`）
- [x] **PLS 直选命中率 7期0%** — 确认为彩票随机性，非 bug。100期回测所有策略直选命中率1-4%。推送文案已标注 Top5 为"参考"

### 推送 & 监控

- [ ] **KL8 check.py 异常主动推送** — 当前 `deliver=local`，异常时不会通知。可改为 `deliver=origin` 或包装成 `kl8_check_push.sh`

---

## 三、后续可以考虑新增（P2 优先级，数据积累后做）

> 功能方向已确认，但需要更多数据或当前不急。

### KL8 模块

- [ ] **多策略对比报告** — v0随机/v1热冷/v2分区/v3遗漏，30 天后跑一次完整对比
- [ ] **stats.py 全量遗漏表** — 当前只有 top10，扩展为全量 80 个号码
- [ ] **check.py `--stage` 参数** — 区分 predict/review/full 检查场景
- [ ] **推送模板加奇偶/大小/连号展示** — stats 已有数据，模板可扩展

### 工程化

- [ ] **遗漏计算真正向量化** — 去除 Python `for` 循环（当前 7600 行性能足够）
- [ ] **GitHub Actions 每日自动运行**（可选，目前 Hermes cron 已覆盖）
- [ ] **五码/六码组选扩展方案** — 从 Top30 提取高频数字池，展开组六/组三组合作为单注推荐的互补维度（方案已有，~30 行代码，详见 memory）

### 数据源

- [ ] **Scrapling 爬虫兜底源** — 方案已设计（见 [2026-05-22 改造记录](2026-05-22-项目稳定化改造记录.md)），等现有数据源连续故障时再接入。只做 KL8 第三数据源兜底，环境变量控制，默认关闭

---

## 四、暂不启用（已实现但不活跃）

> 这些代码已存在，但因为和主链路重叠、条件不成熟、或被降级而不在活跃使用中。

| 文件 | 原因 | 后续 |
|------|------|------|
| `scripts/kl8/strategy.py` | 4 策略统一接口（v0/v1/v2/v3），待回测评估后启用 | 等 backtest.py 建好后评估 |
| `scripts/filter_engine.py` | 轻量预过滤已降级，主要逻辑移至 scoring_engine.py 评分体系 | 保留，必要时恢复 |
| `scripts/push_review_event_if_needed.py` | 与主复盘链路功能重叠，且存在扫描全历史导致旧事件重复触发的风险 | 需重写后再启用 |
| `scripts/kl8/` 独立模块 | 不接入 run_daily.py，保持与 PLS/D3 隔离 | 设计决策，不改 |

---

## 五、不考虑做（明确拒绝，避免反复讨论）

> 以下方案已经过讨论并明确否决，保留原因记录以免未来重复评估。

| 方案 | 否决原因 | 来源 |
|------|---------|------|
| **LSTM/ML/深度学习预测号码** | 彩票无时间依赖，ML 不优于统计方法；服务器无 GPU（2核/3.5GB）；ROI 趋近随机策略 | CLAUDE.md / README.md / PROJECT_REVIEW.md |
| **KL8 接入 run_daily.py** | 独立模块设计不改，避免和 PLS/D3 耦合 | CLAUDE.md |
| **Streamlit 网页面板** | 飞书推送已够用，增加维护成本 | CLAUDE.md |
| **config/kl8.yaml 配置文件** | 常量少，硬编码更清晰 | CLAUDE.md |
| **Scrapling 爬虫（当前接入）** | 现有数据源已稳定，多一个依赖多一个故障点。方案已记录，故障时再接入 | 2026-05-22 讨论 |
| **统一改用 cwl.gov.cn API** | 服务器上返回 403（WAF 防护），不可用 | PROJECT_REVIEW.md |
| **requirements.txt 版本锁定** | 项目运行在 Hermes venv 上，保持版本开放可安装最新兼容版本 | PROJECT_REVIEW.md |
| **添加 plotly/seaborn/tqdm 依赖** | 当前代码未使用，matplotlib 已满足基础需求 | PROJECT_REVIEW.md |
| **软链接部署 Hermes 脚本** | Hermes 安全策略拦截指向 `~/.hermes/scripts/` 外部的软链接，改用实体文件复制 | 2026-05-21 实践结论 |
| **用 Grok 简化版替换 feature_engine/scoring_engine** | 删除了核心功能（数据检查、多样性惩罚、冷号补偿等）且引入了错误 | PROJECT_REVIEW.md |

---

## 六、已完成（从旧清单中勾掉）

> 已解决的历史项目，保留在此以便追溯。

### 2026-05-23（v2.15.2：P1 评分修复 + 止损护栏 + 健康检查优化）

- [x] **P1-1 展示歧义修复** — `scoring_engine.py`：`_add_reason()` 展示格式从 `376(组六)` 改为 `直选376｜组选367`；`generate_predictions()` 新增 assert 校验 `group_number == sorted(号码)`
- [x] **P1-2 日期缺失降级** — `feature_engine.py`：`check_data()` 新增 `警告` 字段，日期列空值从 ❌异常 降级为 ⚠️警告；`print_check_report()` 新增警告输出区，不影响 `通过` 判定
- [x] **P1-3 tune_weights 止损护栏** — `tune_weights.py`：
  - `composite_score()` 新增 3 项止损扣分（ROI<随机中位数 -5、ROI<-10% -10、连未>20 -3）
  - `benchmark_random()` 新增随机策略多 seed 基线评估（输出 mean/median/p25/p75/max_miss_p75）
  - 最佳权重输出路径从 `rules/` 改为 `output/tuning/`（候选文件，不覆盖正式权重）
  - 新增上线门槛：必须 ROI ≥ 随机中位数 + 跨窗口稳定性 ≤ 50% 差异
  - 新增 `--baseline-seeds` / `--no-baseline` 参数
- [x] **P1-4 walk_forward 种子参数** — `backtest.py`：`walk_forward()` 新增 `seed` 可选参数（默认 None=固定42），支持多 seed 随机基线
- [x] **P2-1 健康检查 fallback 口径** — `source_health.py`：新增 `validate_data_quality()` 数据质量校验（期号/号码/行数）；主源异常但数据正常时显示"fallback生效"而非误报失败
- [x] **GPT 审查方案对比** — 逐条对比两套方案后确认：GPT 的安全设计思路（止损+基准+候选文件）优于初版方案，但具体代码需按现有结构实现而非直接粘贴
- [x] **文档同步** — 待处理问题清单、已完成记录、验收结论全部更新至本文档

**本次提交：** `f7c2961`（5 文件，+191/-32 行）

### 2026-05-22（v2.15.1：Linux 服务器端全链路实跑验收）

- [x] **服务器端实跑验收** — 2026-05-22 23:00 在 Linux 上跑通全部 21 项，详见 `output/tests/runtime_test_record.md`
  - 数据抓取 PLS/D3/KL8 ✅ | 特征工程(121维) PLS/D3 ✅ | 统计引擎 PLS/D3 ✅
  - 评分预测(三策略×2) PLS/D3 ✅ | KL8 预测 ✅ | 复盘对比+摘要 ✅
  - 回测(walk-forward 100期) PLS/D3 ✅ | 数据源健康报告 ✅ | 权重调优(随机5轮) ✅
- [x] **项目 venv 依赖安装** — 阿里云镜像安装 pandas/numpy/matplotlib/requests/pyyaml/lxml/optuna 等全部依赖
- [x] **推送脚本同步确认** — 5个脚本 md5 完全一致
- [x] **发现 P1 问题 3 项**：PLS 评分第30名号码展示错位 `376(367)`、PLS 7期直选命中0%、PLS 回测 ROI 不如随机
- [x] **发现 P2 问题 4 项**：PLSjslottery fallback、7591行无日期、备份源未实战验证、PyPI官方不通需阿里云镜像

### 2026-05-22（v2.15.0 第三轮：Windows 兼容 + 本地验证）

- [x] **Windows GBK emoji 崩溃** — 6 个文件代码日志 emoji 改 `[OK]`/`[WARN]`/`[ERR]`
- [x] **subprocess 缺 UTF-8 环境变量** — 6 个文件 `subprocess.run` 统一加 `PYTHONUTF8=1`/`PYTHONIOENCODING=utf-8`
- [x] **Windows 本地验证通过** — data_fetcher/run_daily/daily_review/lottery_review_job 全部 exit 0

### 2026-05-22（v2.15.0 第二轮：GPT 审查修复）

- [x] **shell set -e 异常时日志不完整** — 4 个 push shell 用 `set +e` 包裹 Python 调用
- [x] **lottery_review_job.py final 兜底 --force 绕过去重** — 改为 `--final-check`
- [x] **Job 过程日志混入 stdout** — 4 个 job 全部过程日志改 `print(..., file=sys.stderr)`
- [x] **文件换行格式风险** — 新建 `.gitattributes` 强制 LF 换行
- [x] **README 缺正式入口说明** — 新增"正式运行入口"章节

### 2026-05-22（v2.15.0 第一轮：Job 架构改造）

- [x] **预测链路 cron 统一** — 删除 14:30/14:35 旧任务，14:40 为单一 Job 入口（lottery_predict_job.py 自闭环：run_daily + source_health + hermes_push）
- [x] **KL8 复盘旧文件误推** — `kl8_review_job.py` 删旧文件+时间戳+期号三重校验
- [x] **PLS/D3 复盘失败被吞掉** — `daily_review.py` 返回值聚合，异常 exit 2 阻断推送
- [x] **去重从文本 hash 升级为业务键** — `hermes_push.py` 新增 `already_sent_by_key` + `--dedup-key`
- [x] **晚间三波任务重叠执行** — shell 顶部 `flock -n` 全流程锁
- [x] **cron 推送 --force 绕过去重** — 4 个 push shell 全部去掉 `--force`
- [x] **Shell 脚本臃肿** — 4 个 shell 瘦身为薄入口，业务逻辑迁移到 4 个 Python job
- [x] **状态不可追踪** — 新增 `output/status/*.json` + `job_status.py`
- [x] **HERMES_CONFIG.md 路径不一致** — `/path/to` 全部替换为真实路径
- [x] **文档过时** — CHANGELOG / CLAUDE / README / Makefile 全部同步到 v2.15.0

### 2026-05-21（v2.14.0）

- [x] **KL8 推送脚本文件缺失** — `~/.hermes/scripts/` 新建实体文件
- [x] **lottery_review_push.sh 旧版未同步** — 同步新版（日志/--complete-only/--final-check）
- [x] **软链接被 Hermes 拦截** — 改为实体文件复制，确立维护方式

### 2026-05-21（v2.13.0）

- [x] **晚间复盘完整性闸门** — 双彩种齐全才推送，--complete-only/--final-check 区分波浪
- [x] **文件锁 stale_after 优化** — 拆分为 5s 等待 / 600s 过期，防止并发重复推送
- [x] **review_push.sh stderr 丢弃** — 重写为追加入日志文件

---

---

## 七、v2.15.2 最终验收（2026-05-23）

> 基于 GitHub remote main `f7c2961`。本轮修复了 P1 评分展示 + 止损护栏 + 健康检查，之后进入实盘观察。

### 本两轮 session 提交链

| 提交 | 说明 |
|------|------|
| `d759798` | fix: job 层 hermes_push 失败不再被静默吞掉 |
| `8a3c3ea` | docs: 修正 HERMES_CONFIG.md 晚间复盘链路描述 |
| `fcc9bf2` | docs: 新增 v2.15.1 最终验收结论 + 实盘观察清单 |
| `f7c2961` | **fix: 评分展示+止损护栏+健康检查 三项修复（P1+P2）** — 5 文件 +191/-32 |
| `cce2751` | docs: Python 环境约定 + PyPI 源（服务器推的） |
| `3303051` | docs: 全链路实跑测试记录 v2.15.1（服务器推的） |

### 最终验收清单

| 项目 | 状态 | 核实方式 |
|------|:--:|------|
| 4 个 push shell `set +e`/`set -e` | ✅ | GitHub raw 逐文件确认 |
| 4 个 push shell Job 架构 | ✅ | 全部调用 `scripts/jobs/*.py` |
| `hermes_push.py` 5 个 CLI 参数 | ✅ | `--lottery` `--stdout` `--complete-only` `--final-check` `--dedup-key` |
| final 兜底链路 | ✅ | `--final-check` → `check_review_ready()` → 输出兜底文本 → exit 0 |
| `HERMES_CONFIG.md` 文档 | ✅ | 已修正为 Job 编排架构 |
| 展示歧义修复 | ✅ | `直选376｜组选367` + assert 校验 |
| 日期缺失降级 | ✅ | ❌→⚠️，不影响通过判定 |
| tune_weights 止损护栏 | ✅ | 3 项扣分 + 随机基线 + 候选文件 + 上线门槛 |
| 健康检查 fallback 口径 | ✅ | 数据质量校验，fallback 生效不误报 |
| Python 语法检查 | ✅ | 全部 5 文件通过 `py_compile` |

### 实盘观察清单

> 在 Linux 服务器上执行。跑完一轮 cron 后检查。

```bash
cd /home/admin/bendi/lottery-analysis
git pull   # 确认在 f7c2961 或更新

# ── 日志 ──
tail -n 120 logs/push/lottery_predict_$(date +%F).log
tail -n 160 logs/push/lottery_review_$(date +%F).log
tail -n 120 logs/push/kl8_predict_$(date +%F).log
tail -n 160 logs/push/kl8_review_$(date +%F).log

# ── 状态文件 ──
cat output/status/lottery_predict.json
cat output/status/lottery_review.json
cat output/status/kl8_predict.json
cat output/status/kl8_review.json
```

### 观察重点

1. **推送次数** — 每种彩种每种模式每天最多推送 1 次，重复执行被 dedup_key 跳过
2. **final 兜底** — 23:10 若开奖未齐，是否真的推送了"无法完成复盘"通知
3. **status.json** — 每次执行是否写入了正确的 status / dedup_key / reason
4. **exit code** — 正常路径 exit 0，异常路径 exit 2，环境错误 exit 3
5. **展示格式** — 推送消息中候选号码是否显示为 `直选XXX｜组选XXX` 新格式
6. **健康报告** — PLS fallback 是否不再误报为异常

### PLS 权重调优（实盘稳定后再跑）

```bash
# 等 review_history 积累更多数据后执行
.venv/bin/python scripts/tune_weights.py \
  --lottery pls --method optuna --trials 80 --periods 80 --top-k 30

# 如果候选权重通过全部安全检查，手动上线：
# cp output/tuning/scoring_weights_pls_candidate_*.yaml rules/scoring_weights_pls_tuned.yaml
```

### 当前策略

**冻结代码，进入实盘观察。PLS 权重调优等积累更多复盘数据后再跑。**

---

> **维护说明：** 此文件应随项目演进持续更新。新增想法时先在对应章节加 `- [ ]`，完成时打 `- [x]` 并移至"已完成"章节。否决方案时加入"不考虑做"章节并注明原因。
